<!DOCTYPE html>
<html class="fontawesome-i2svg-active fontawesomei2svg-complete">
  <head>

  </head>

  <body>
    <header>

    </header>

    <main>
      <p id="notice"></p>
      <div class="container px-5 px-sm-0">
        <div class="row">
          <div class="col-md-3">

            <h2>User info</h2>

            <img src="">

            <table class="table">
              <tbody>
                <tr></tr>

                <% @user(:id).each do |user| %>

                <tr>
                  <th>name</th>
                  <th><%= @user_name %></th>
                </tr>
                <tr>
                  <th>introduction</th>
                  <th><%= @user_name %></th>
                </tr>
              </tbody>
            </table>

            <div class="row">
              <a class=<% link_to "", edit_user_path(user.id) %>></a>
            </div>

            <h2 class="mt-3">New book</h2>

            <div class="col-md-8 offset-md-1">
              <h2>Books</h2>
              <table class="table table-hover table-inverse">

                <thead>
                  <tr>
                    <th></th>
                    <th>Title</th>
                    <th>Opinion</th>
                    <th colspan="3"></th>
                  </tr>
                </thead>

                <tbody>
                  <% @books.each do |book| %>
                  <tr>
                    <td><%= book.title %></td>
                    <td><%= book.body %></td></td>
                  </tr>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </main>

    <footer>

    </footer>

  </body>
</html>